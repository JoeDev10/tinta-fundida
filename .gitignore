# ── El panel NO se publica ──────────────────────────────────
# Vive solo en la computadora del dueño. Si no está en el
# servidor, nadie puede entrar aunque adivine la dirección.
admin.html
css/admin.css
js/admin.js
js/clave.js

# Las pruebas SÍ van al repo: son las que corren solas en cada push.
# Como GitHub Pages sirve todo lo que está en el repo, la carpeta tests/
# también queda publicada. No es un problema —no hay nada secreto ahí—
# pero por eso el panel sigue afuera: si admin.html entrara acá, quedaría
# vivo en internet, y ahí sí la clave dejaría de servir de algo.

# herramientas locales
.claude/
node_modules/

# sistema
Thumbs.db
desktop.ini
.DS_Store
